---
import ArchivePanel from "@components/ArchivePanel.svelte";
import I18nKey from "@i18n/i18nKey";
import { i18n } from "@i18n/translation";
import MainGridLayout from "@layouts/MainGridLayout.astro";
import { getVisibleBlogPostList } from "../domains/blog/content/query";

export const prerender = false;

const canViewPrivateContent = Astro.locals.canViewPrivateContent === true;
const sortedPostsList = await getVisibleBlogPostList(canViewPrivateContent);
---

<MainGridLayout title={i18n(I18nKey.archive)}>
    <ArchivePanel sortedPosts={sortedPostsList} client:only="svelte"></ArchivePanel>
</MainGridLayout>
